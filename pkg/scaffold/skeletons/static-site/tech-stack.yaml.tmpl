name: {{ .Name }}
ingress:
  host: {{ .Domain }}
services:
  - name: {{ .Name }}
    image: {{ .Registry }}/{{ .Name }}:latest
    port: 80
    resources:
      cpu: 100m
      memory: 64Mi
    ingress:
      enabled: true
      path: /{{ .Name }}
